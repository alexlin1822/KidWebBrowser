import {
  GetStorageKey,
  GenerateNewId,
  SetCurrentID,
  GetCurrentID,
} from "./Common";

/* Remote Storage */

export async function SaveNewData_remote(keyname, content) {}

export async function SaveUpdateData_remote(keyname, content) {}

export async function deleteData_remote(keyname, content) {}

export async function LoadData_remote(datatype, keyname) {}

export async function LoadAccountData_remote(username, password) {}

export async function CheckAccountExist_remote(username) {}
